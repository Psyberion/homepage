/*
 * Defines what colours the background of the headers of each link-block
 * should have. The colours are chosen using `colors[i%colors.length]`,
 * so the array can be of arbitrary length
 */
colors = [
		"darkgrey",
		];
